export interface JobRangeOption {
  value: string
  label: string
  min?: number
  max?: number
}

/**
 * ช่วงจำนวนเที่ยววิ่ง — ใช้ร่วมกันทั้งหน้ารายชื่อ พขร. และหน้ารอประเมิน เพื่อให้ผู้ใช้
 * เจอตัวเลือกชุดเดียวกันทุกที่ ขอบเขตซ้อนกันตรงตัวเลขหัว-ท้ายตั้งใจ (10 อยู่ได้ทั้ง
 * "1-10" และ "10-20") เพื่อให้ป้ายกำกับตรงกับตัวเลขที่พิมพ์ไว้ตรง ๆ
 * ช่วงสุดท้ายเปิดปลายบน (ไม่มี max) เพราะมี พขร. บางคนวิ่งเกิน 300 เที่ยว
 * ถ้าปิดปลายไว้คนกลุ่มนี้จะไม่มีตัวเลือกไหนกรองถึงเลย
 */
export const JOB_RANGE_OPTIONS: JobRangeOption[] = [
  { value: '', label: 'ไม่มีกำหนด' },
  { value: '1-10', label: '1-10 เที่ยว', min: 1, max: 10 },
  { value: '10-20', label: '10-20 เที่ยว', min: 10, max: 20 },
  { value: '20-100', label: '20-100 เที่ยว', min: 20, max: 100 },
  { value: '100-200', label: '100-200 เที่ยว', min: 100, max: 200 },
  { value: '200+', label: '200 เที่ยวขึ้นไป', min: 200 },
]
