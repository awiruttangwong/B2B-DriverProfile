-- =============================================================================
-- 0015_rating_reason_optional.sql
--
-- เอาข้อบังคับ "เหตุผลต้องยาวอย่างน้อย 20 ตัวอักษร" ออก ตามที่ผู้ใช้แจ้งว่าไม่ควร
-- บังคับกรอกเหตุผลตอนประเมิน — คอลัมน์ยังเป็น not null เหมือนเดิม (หน้าเว็บส่ง
-- ค่าว่าง '' เสมอ ไม่เคยส่ง null) แค่ไม่บังคับความยาวขั้นต่ำอีกต่อไป
-- =============================================================================

alter table driver_ratings drop constraint if exists driver_ratings_reason_len;
